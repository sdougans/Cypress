{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "cy:open": "npx cypress open --e2e -b chrome",
    "cy:test": "npx cypress run",
    "cy:base": "npx cypress run --env visualRegressionType=base",
    "cy:actual": "npx cypress run --env visualRegressionType=regression"
  },
  "devDependencies": {
    "cypress": "^13.13.0",
    "cypress-visual-regression": "^5.0.2"
  }
}
